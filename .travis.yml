language: python
python:
  - "2.7"
  - "3.5"
  - "3.6"
  - "3.7"
before_install:
  - ls -l /usr/bin
  - sudo /usr/bin/pip install black
install:
  - pip install -r requirements.txt
script:
  - /usr/bin/black --check matplotboard
